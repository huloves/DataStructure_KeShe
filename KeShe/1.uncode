#include <stdio.h>
#include <math.h>

int judge(double t1, double t2, double t);

int main()
{
    double t1, t, t2;

    scanf("%lf %lf", &t1, &t);

    

    return 0;
}

int judge(double t1, double t2, double t)
{
    if((t2-t1)/(log(t2)-log(t1)) > t) {
        return 1;
    }
    else if((t2-t1)/(log(t2)-log(t1)) < t) {
        return 2;
    }
    else {
        return 0;
    }
}
